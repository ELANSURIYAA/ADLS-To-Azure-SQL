=============================================

Author: Ascendion AAVA

Created on: 

Description: Azure Data Factory Mapping Dataflow Validation Report

=============================================

Azure Dataflow Validation Report

1. Summary

Dataflow Name: Manual_DF_CLIENT_BRONZE_TO_SILVER

Overall Quality Score (%): 97

Justification:
- All logical blocks present and correctly implemented.
- Robust source schema definition and appropriate use of schema drift.
- Join, derived columns, and data quality checks are comprehensive and aligned with business logic.
- Conditional split ensures proper routing of valid and error records.
- Sink and error handling blocks are well-configured.
- Minor potential for improvement in error message enrichment and error record schema.

2. Logical Block Validation

Logical Block	Status	Observations
Source	Valid	Two sources defined with explicit schema, allowSchemaDrift:true, validateSchema:false. Data types and fields appropriate. Isolation level and format specified. No issues found.
Join	Valid	Left join on ClientFullName == SubscriberName. Join type appropriate for use case. No risk of duplication or data loss. MatchType and broadcast settings are sensible.
Derived Columns	Valid	All mappings and business logic (including status mapping, date, and relationship type) are clear and correct. Expression syntax valid. Business intent well-covered.
Data Quality Checks	Valid	Comprehensive iif-based validation for all key fields (null, enum, string length, date comparison, status logic). Error message concatenation logic robust. Enum coverage complete.
Conditional Split	Valid	Split on ValidationErrors length ensures valid/error routing. Disjoint:false is correct for this use case.
Sink	Valid	Valid sink for both TargetClient and Error, schema aligns with transformations. Insertable:true, deletable:false, updateable:false, upsertable:false. ErrorHandlingOption:stopOnFirstError is appropriate.
Error Handling	Valid	Error sink configured with meaningful fields (ErrorID, PipelineName, ActivityName, ErrorMessage, ErrorTime, BatchDate, RecordsProcessed). Schema is sufficient for basic error tracking.

3. Process Flow Assessment

Status: Fully Correct
Observations: Transformation sequence follows best practices: Source → Join → Derived Columns → Data Quality Checks → Conditional Split → Sink. No missing steps, logical consistency maintained throughout.

4. Business Rule Validation

Fully Covered:
- ClientName null/blank check
- IndustrySector enum validation
- HeadquartersCountry length validation
- RevenueRange enum validation
- EmployeeCountRange enum validation
- RelationshipType enum validation
- ContractStartDate null check
- ContractEndDate > ContractStartDate check
- ClientStatus enum validation

Partially Covered:
- Error sink schema could be enriched with more context (e.g., source record reference)

Missing / Needs Review:
- No explicit check for duplicate ClientName or SubscriberName
- No validation of date formats or timestamp consistency
- No explicit business confirmation for enum lists (assumed correct)

5. Risks & Issues

| Item | Description | Severity | Type | Recommendation |
|------|-------------|----------|------|---------------|
| 1 | Error sink lacks source record reference for traceability | Low | Schema | Add SourceRecordID or CRMClientID to error sink |
| 2 | No explicit duplicate check for ClientName | Low | Validation | Add duplicate detection logic if required |
| 3 | No validation for date/timestamp format | Low | Validation | Add date format validation if source variability expected |

6. API Cost

API cost: 0.00012000 USD
