=============================================

Author: Ascendion AAVA

Created on: 

Description: Azure Data Factory Mapping Dataflow Validation Report

=============================================

Azure Dataflow Validation Report

1. Summary

Dataflow Name: MemberDataIntegrationFlow

Overall Quality Score (%): 60

Justification:
- Multiple source datasets with defined schemas, but allowSchemaDrift enabled and validateSchema disabled (risk of schema inconsistency)
- No join transformations present, only splits; possible missing integration logic
- Conditional splits route all records (true condition), lacking explicit validation rules or business logic
- Derived columns only present for AuditLog, no business derivations in main flows
- Sinks configured for each entity, but error handling only via 'stopOnFirstError', no dedicated error sinks for data errors
- No explicit data quality checks, iif conditions, or rule coverage in splits
- Process flow is linear but lacks validation and error routing
- Business rules and validation logic largely missing, only operational metadata in AuditLog
- Risks of data inconsistency, weak validation, and lack of error capture

2. Logical Block Validation

Logical Block	Status	Observations
Source	Valid	Schemas defined, allowSchemaDrift: true (risk), validateSchema: false (risk), fields appropriate
Join	Missing	No joins present, possible missing business integration logic
Derived Columns	Partially Valid	Only AuditLog has derivations; business columns not derived in main flows
Data Quality Checks	Missing	No explicit data quality checks, iif, or validation expressions present
Conditional Split	Invalid	All splits use 'true()', no separation of valid/error records, lacks business logic
Sink	Valid	Sinks configured, schema matches, insertable, errorHandlingOption: stopOnFirstError
Error Handling	Risky	No dedicated error sinks for invalid records, only operational error handling; no error schema

3. Process Flow Assessment

Status: Broken / Incomplete
Observations:
- Expected flow (Source → Join → Derived Columns → Data Quality Checks → Conditional Split → Sink) not followed
- No joins, derived columns missing in main flows, splits lack validation logic
- Error routing for invalid records is not implemented

4. Business Rule Validation

Fully Covered:
- AuditLog operational metadata derivation

Partially Covered:
- Source schema definition

Missing / Needs Review:
- All business validation logic (iif, enum checks, NULL/blank handling, date validation, error message construction)
- Conditional split routing by data quality
- Error record capture and schema

5. Risks & Issues

| Item | Description | Severity | Type | Recommendation |
|------|-------------|----------|------|---------------|
| 1 | allowSchemaDrift enabled on all sources/sinks | Medium | Schema | Justify or restrict drift, enable validateSchema |
| 2 | validateSchema disabled everywhere | High | Schema | Enable schema validation for production |
| 3 | No join transformations | High | Logic | Add joins if business integration needed |
| 4 | Conditional splits with 'true()' only | High | Logic | Add business validation conditions to splits |
| 5 | No data quality checks or iif expressions | High | Validation | Add explicit validation rules |
| 6 | No error handling for invalid records | High | Error Handling | Implement error sinks and error schema |
| 7 | No business rule enforcement | High | Validation | Add business rule logic (enum, NULL, date, etc.) |
| 8 | Sinks use stopOnFirstError only | Medium | Error Handling | Use error sinks for granular error capture |
| 9 | Derived columns only for AuditLog | Medium | Logic | Add business derivations as needed |

6. API Cost

API cost: 0.00012000 USD
